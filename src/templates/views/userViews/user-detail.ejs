<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>User Detail</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</head>

<body>
    <div class="container mt-4">
        <h1>User Detail</h1>
        <form id="userForm" class="row g-3" action="/users/put-user" method="POST">
            <div class="col-md-6">
                <label for="fullname" class="form-label">Full name</label>
                <input type="text" class="form-control" id="fullname" name="fullname" value="<%= user.fullname %>"
                    <%=isEditable ? '' : 'disabled' %>/>
            </div>
            <div class="col-md-6">
                <label for="phoneNumber" class="form-label">Phone number</label>
                <input type="text" class="form-control" id="phoneNumber" name="phoneNumber"
                    value="<%= user.phoneNumber %>" <%=isEditable ? '' : 'disabled' %> />
            </div>
            <div class="col-md-12">
                <label for="address" class="form-label">Address</label>
                <input type="text" class="form-control" id="address" name="address" value="<%= user.address %>"
                    <%=isEditable ? '' : 'disabled' %> />
            </div>
            <div class="col-md-12">
                <label for="image" class="form-label">Image</label>
                <input type="text" class="form-control" id="image" name="image" value="<%= user.image %>" <%=isEditable
                    ? '' : 'disabled' %> />
            </div>
            <input type="hidden" name="id" value="<%= user.id %>" />
            <div class="col-12">
                <% if (isEditable) { %>
                    <button type="submit" class="btn btn-success">Save</button>
                    <a href="/users/<%= user.id %>" class="btn btn-secondary">Cancel</a>
                    <% } else { %>
                        <a href="/users/<%= user.id %>?edit=true" class="btn btn-primary">Edit</a>
                        <% } %>
                            <a href="/users" class="btn btn-danger">Back</a>
            </div>
        </form>
    </div>
</body>

</html>